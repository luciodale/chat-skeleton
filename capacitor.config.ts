import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'lu.skeletop.test.app',
  appName: 'lu-skeleton-test-app',
  webDir: 'dist'
};

export default config;
